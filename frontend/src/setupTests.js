// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';

// Optional: configure user-event defaults if needed
// import { configure } from '@testing-library/user-event'
// configure({ pointerEventsCheck: 0 });

// Optional: jest-axe global helpers
// If you want to use jest-axe across tests, uncomment and ensure jest-axe is installed
// import 'jest-axe/extend-expect';
