// Place this file in the frontend/ directory to configure React for GitHub Pages deployment
const { defineConfig } = require('react-scripts/config');

module.exports = defineConfig({
  publicPath: '/Autonomous-AI-Med-Agent/',
});
